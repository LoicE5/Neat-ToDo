{
    "version": 2,
    "builds": [
        {
            "src": "frontend/next.config.js",
            "use": "@vercel/next"
        },
        {
            "src": "server/build/index.js",
            "use": "@vercel/node",
            "config": { "includeFiles": ["server/build/**"] }
        }
    ],
    "routes": [
        {
          "src": "/server/(.*)",
          "dest": "/api"
        },
        {
          "src": "/(.*)",
          "dest": "/frontend/$1"
        }
    ]
}