{
    "version": 2,
    "builds": [
        {
            "src": "api/index.js",
            "use": "@vercel/node",
            "config": { "includeFiles": ["server/build/**"] }
        },
        {
            "src": "frontend/next.config.js",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        {
          "src": "/server/(.*)",
          "dest": "/"
        },
        {
          "src": "/(.*)",
          "dest": "/frontend/$1"
        }
    ]
}